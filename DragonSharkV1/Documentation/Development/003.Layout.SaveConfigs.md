There are many save configs in the `/home/pi/.config/retroarch/config/` directory.

1. `atari2600.cfg`:

```
savestate_directory = "/mnt/SAVES/atari2600"
```

2. `atari5200.cfg`:

```
savestate_directory = "/mnt/SAVES/atari5200"
```

3. `atari7800.cfg`:

```
savestate_directory = "/mnt/SAVES/atari7800"
```

4. `atarijaguar.cfg`:

```
savestate_directory = "/mnt/SAVES/atarijaguar"
```

5. `atarilynx.cfg`:

```
savestate_directory = "/mnt/SAVES/atarilynx"
```

6. `desmume2015.cfg`:

```
savestate_directory = "/mnt/SAVES/nds"
```

7. `dosbox.cfg`:

```
savestate_directory = "/mnt/SAVES/dosbox"
```

8. `gamegear.cfg`:

```
savestate_directory = "/mnt/SAVES/gamegear"
```

9. `gba.cfg`:

```
savestate_directory = "/mnt/SAVES/gba"
```

10. `gbc.cfg`:

```
savestate_directory = "/mnt/SAVES/gbcolor"
```

11. `genesis.cfg`:

```
savestate_directory = "/mnt/SAVES/genesis"
```

12. `mame.cfg`:

```
savestate_directory = "/mnt/SAVES/mame"
```

13. `mastersystem.cfg`:

```
savestate_directory = "/mnt/SAVES/mastersystem"
```

14. `mupen64plus.cfg`:

```
savestate_directory = "/mnt/SAVES/n64"
```

15. `neogeocd.cfg`:

```
savestate_directory = "/mnt/SAVES/neogeocd"
```

16. `neogeopocket.cfg`:

```
savestate_directory = "/mnt/SAVES/neogeopocket"
```

17. `nes.cfg`:

```
savestate_directory = "/mnt/SAVES/nes"
```

18. `ps1.cfg`:

```
savestate_directory = "/mnt/SAVES/ps1"
```

19. `segacd.cfg`:

```
savestate_directory = "/mnt/SAVES/segacd"
```

20. `snes.cfg`:

```
savestate_directory = "/mnt/SAVES/snes"
```
