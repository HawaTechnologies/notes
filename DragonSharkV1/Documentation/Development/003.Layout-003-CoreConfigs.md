There are many save configs in the `/home/pi/.config/retroarch/config/` directory.

1. `atari2600.cfg`:

```
savestate_directory = "/mnt/SAVES/atari2600/states"
savefile_directory = "/mnt/SAVES/atari2600/saves"
```

2. `atari5200.cfg`:

```
savestate_directory = "/mnt/SAVES/atari5200/states"
savefile_directory = "/mnt/SAVES/atari5200/saves"
```

3. `atari7800.cfg`:

```
savestate_directory = "/mnt/SAVES/atari7800/states"
savefile_directory = "/mnt/SAVES/atari7800/saves"
```

4. `atarijaguar.cfg`:

```
savestate_directory = "/mnt/SAVES/atarijaguar/states"
savefile_directory = "/mnt/SAVES/atarijaguar/saves"
```

5. `atarilynx.cfg`:

```
savestate_directory = "/mnt/SAVES/atarilynx/states"
savefile_directory = "/mnt/SAVES/atarilynx/saves"
```

6. `nds.cfg`:

```
savestate_directory = "/mnt/SAVES/nds/states"
savefile_directory = "/mnt/SAVES/nds/saves"
```

7. `dosbox.cfg`:

```
savestate_directory = "/mnt/SAVES/dosbox/states"
savefile_directory = "/mnt/SAVES/dosbox/saves"
```

8. `gamegear.cfg`:

```
savestate_directory = "/mnt/SAVES/gamegear/states"
savefile_directory = "/mnt/SAVES/gamegear/saves"
```

9. `gba.cfg`:

```
savestate_directory = "/mnt/SAVES/gba/states"
savefile_directory = "/mnt/SAVES/gba/saves"
```

10. `gbc.cfg`:

```
savestate_directory = "/mnt/SAVES/gbcolor/states"
savefile_directory = "/mnt/SAVES/gbcolor/saves"
```

11. `genesis.cfg`:

```
savestate_directory = "/mnt/SAVES/genesis/states"
savefile_directory = "/mnt/SAVES/genesis/saves"
```

12. `mame.cfg`:

```
savestate_directory = "/mnt/SAVES/mame/states"
savefile_directory = "/mnt/SAVES/mame/saves"
```

13. `mastersystem.cfg`:

```
savestate_directory = "/mnt/SAVES/mastersystem/states"
savefile_directory = "/mnt/SAVES/mastersystem/saves"
```

14. `n64.cfg`:

```
savestate_directory = "/mnt/SAVES/n64/states"
savefile_directory = "/mnt/SAVES/n64/saves"
mupen64plus-video-plugin = "mupen64plus-video-gliden64"
mupen64plus-video-RDPi-resolution-factor = "1"
mupen64plus-cpucore = "dynamic_recompiler"
mupen64plus-CountPerOp = "1"
mupen64plus-GLideN64-UseNativeResolutionFactor = "0"
mupen64plus-GLideN64-EnableFBEmulation = "False"
mupen64plus-GLideN64-EnableCopyColorToRDRAM = "Disable"
mupen64plus-GLideN64-EnableCopyDepthToRDRAM = "Disable"
mupen64plus-GLideN64-EnableHybridFilter = "False"
mupen64plus-GLideN64-enableFog = "False"
```

15. `neogeo.cfg`:

```
savestate_directory = "/mnt/SAVES/neogeo/states"
savefile_directory = "/mnt/SAVES/neogeo/saves"
```

16. `neogeocd.cfg`:

```
savestate_directory = "/mnt/SAVES/neogeocd/states"
savefile_directory = "/mnt/SAVES/neogeocd/saves"
```

17. `neogeopocket.cfg`:

```
savestate_directory = "/mnt/SAVES/neogeopocket/states"
savefile_directory = "/mnt/SAVES/neogeopocket/saves"
```

18. `nes.cfg`:

```
savestate_directory = "/mnt/SAVES/nes/states"
savefile_directory = "/mnt/SAVES/nes/saves"
```

19. `ps1.cfg`:

```
savestate_directory = "/mnt/SAVES/ps1/states"
savefile_directory = "/mnt/SAVES/ps1/saves"
```

20. `segacd.cfg`:

```
savestate_directory = "/mnt/SAVES/segacd/states"
savefile_directory = "/mnt/SAVES/segacd/saves"
```

21. `snes.cfg`:

```
savestate_directory = "/mnt/SAVES/snes/states"
savefile_directory = "/mnt/SAVES/snes/saves"
```

Also, for the general ~/.config/retroarch/retroarch.cfg file, replace the following settings:

```
autosave_interval = "5"
```

(leave all the others unaffected)
